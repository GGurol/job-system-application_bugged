# .env

# PostgreSQL Database Credentials
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=jobapp

# This URL is used by the backend to connect to the DB container
DATABASE_URL=postgresql://postgres:postgres@db:5432/jobapp

# This URL is used by the frontend to connect to the backend container
NEXT_PUBLIC_API_URL=http://localhost:3001 # Corrected port
CORS_ORIGIN=http://localhost:3000

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Other variables can be left empty for now
REDIS_URL=
UPLOAD_PATH=./uploads
AWS_S3_BUCKET=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=